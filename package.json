{
  "name": "xcx-my-extension",
  "version": "0.0.0",
  "private": "true",
  "scripts": {
    "register": "npx xcratch-register --link -C --id=myExtension --gui=\"../scratch-gui\"",
    "build": "npx xcratch-build --module=myExtension --gui=\"../scratch-gui\""
  },
  "devDependencies": {
    "eslint-config-scratch": "^7.0.0"
  }
}
